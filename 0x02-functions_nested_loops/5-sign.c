#include "main.h"

/**
 * _isalpha - Entry point
 * @c: character check
 * Return: 1 for aphabetic character or 0 for any
 */
int print_sign(int n)
{
	if (n > 0)
	{
		_putchar('+');
		return (1);
	} else if (n == 0)
}
		_putchar(48);
		return (0);
	} else if (n < 0)
	{
		_putchar('-');
	}
		return (-1);
}


